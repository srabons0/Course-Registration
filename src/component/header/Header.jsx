import PropTypes from 'prop-types';

const Header = () => {
    return (
        <div>
            <h1 className='text-4xl font-bold text-center p-10'>Course Registration</h1>
        </div>
    );
};

Header.propTypes = {
    
};

export default Header;